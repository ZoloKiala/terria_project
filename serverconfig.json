{
  // Fallback only; Railway's injected $PORT is passed via --port at startup.
  "port": 3001,

  // Railway terminates TLS and reverse-proxies to the container. Trusting the
  // X-Forwarded-* headers makes terriajs-server generate correct https share URLs.
  "trustProxy": true,

  "allowProxyFor": [
    // The IWMI Data Cube explorer API serves GeoJSON without CORS headers, so vector
    // items from it are fetched via this server's /proxy endpoint. Matching is exact
    // hostname or *.domain, so this allows only this one host.
    "explorer-production-0070.up.railway.app",

    // The Water Accounting Plus COGs live on this Cloudflare R2 bucket, which sends no
    // CORS headers and answers OPTIONS preflight with 403, so the browser cannot read
    // them cross-origin. COG range requests are proxied through this server instead.
    // (The dem_limpopo S3 bucket does send Access-Control-Allow-Origin: * and is read
    // directly, so it deliberately is not listed here.)
    "pub-43d699efb2a2495eb85a4b417443e541.r2.dev",

    // One et_monthly_limpopo time step is served from this bucket. It is readable but,
    // unlike the -public bucket, returns no CORS headers, so that layer is proxied.
    "copy-iwmi-dt-odc-products-private.s3.af-south-1.amazonaws.com",

    // datacube-ows, which serves the time-enabled (animated) WMS layers. It sends no CORS
    // headers, and Cesium loads imagery into WebGL textures, which requires them -- so both
    // GetCapabilities and the tiles go through this server's proxy.
    "ows-production-108d.up.railway.app",

    "githubusercontent.com",

    // NOTE: The following default entries are deprecated and will be removed in a future release. If you rely ony terria proxying to any of these servers, please add them through a custom serverconfig.json. See https://docs.terria.io/guide/getting-started/#customizing-terriamap
    "nicta.com.au",
    "gov.au",
    "csiro.au",
    "argo.jcommops.org",
    "www.abc.net.au",
    "geoserver.aurin.org.au",
    "mapsengine.google.com",
    "s3-ap-southeast-2.amazonaws.com",
    "adelaidecitycouncil.com",
    "www.dptiapps.com.au",
    "geoserver-123.aodn.org.au",
    "geoserver.imos.org.au",
    "nci.org.au",
    "static.nationalmap.nicta.com.au",
    "gov",
    "gov.uk",
    "gov.nz",
    "services.aremi.data61.io"
  ]
}
